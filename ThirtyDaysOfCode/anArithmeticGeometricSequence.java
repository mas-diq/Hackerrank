import java.util.Scanner;

public class anArithmeticGeometricSequence {
    public static void main(String[] args) {
        Scanner s = new Scanner(System.in);
        int a = s.nextInt();
        int d = s.nextInt();
        int n = s.nextInt();
        // int x = s.nextInt();
        // int[] arr = new int[a];
        // double sum = 0;
        // for (int i = 1; i <= n; i++) {
        //     sum = ((i / (a - d * i) / ((i / (a - d * i) * i))));
        // }
        s.close();
    }
}